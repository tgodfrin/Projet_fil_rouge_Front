// Dev environment — used by `ng serve` and `ng build --configuration development`.
// The back-end runs locally on port 8080 with the /api context-path.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
};
